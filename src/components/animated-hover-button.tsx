"use client"

import { Button } from "./ui/button";

interface AnimatedButtonProps {
  
}

export const AnimatedButton = ({} : AnimatedButtonProps) => {
   
  return (
     <Button variant="secondary" size="icon">
        AnimatedButton
     </Button>
    );
}
